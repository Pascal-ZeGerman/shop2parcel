name: hassfest

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  hassfest:
    name: Validate manifest.json
    uses: "home-assistant/actions/.github/workflows/hassfest.yml@master"
