---
phase: 13.1-sensor-restore-on-restart-extend-dedup-store-to-persist-ship
plan: 02
subsystem: ha-storage
tags: [ha-storage, coordinator, migration, tdd-green, dataclasses, persistence]

# Dependency graph
requires:
  - phase: 13.1-01
    provides: "6 RED tests covering store persistence and restore contracts (tests a-f)"

provides:
  - "STORAGE_VERSION = 3 with v2->v3 migration branch in _async_migrate_func"
  - "_SHIPMENT_FIELD_TYPES module-level constant for per-entry validation"
  - "_pending_shipments and _restored_shipments fields on Shop2ParcelCoordinator"
  - "_async_load_store extended with resilient persisted_shipments loading"
  - "_async_save_store lambda emits persisted_shipments via asdict()"
  - "async_cleanup_delivered persists post-cleanup state to store"

affects:
  - 13.1-03
  - coordinator
  - imap_coordinator
  - gmail_coordinator

# Tech tracking
tech-stack:
  added: ["dataclasses.asdict (already bundled, newly imported)"]
  patterns:
    - "Per-entry type validation with _SHIPMENT_FIELD_TYPES map (T-13.1-04 ASVS V5)"
    - "Debounced store save via async_delay_save lambda capturing self by closure"
    - "v2->v3 migration preserves existing keys, seeds new key as empty dict"

key-files:
  created: []
  modified:
    - custom_components/shop2parcel/coordinator.py

key-decisions:
  - "STORAGE_VERSION bumped from 2 to 3 with explicit v2->v3 migration branch that preserves submitted_tracking_numbers + quota_exhausted_until, seeds persisted_shipments as {}"
  - "_SHIPMENT_FIELD_TYPES defined at module level (not inline) for reuse and static analysis visibility"
  - "persisted_shipments lambda uses closure capture of self (not snapshot) so any update to _pending_shipments before debounce fires is reflected in the write"
  - "_pending_shipments assigned BEFORE awaiting _async_save_store() in cleanup so lambda reads the updated dict"

patterns-established:
  - "Per-entry store validation pattern: fetch raw dict, iterate entries, check isinstance(entry, dict) + all field types via _SHIPMENT_FIELD_TYPES, emit WARNING per invalid entry, reconstruct dataclass"
  - "Stale same-major passthrough test must be updated when STORAGE_VERSION bumps (version N now tests passthrough for old_major_version=N, not N-1)"

requirements-completed: [R3, R5, R4, R1]

# Metrics
duration: 18min
completed: 2026-05-23
---

# Phase 13.1 Plan 02: Store Schema v3 + Base-Coordinator Persistence Summary

**STORAGE_VERSION bumped to 3 with v2->v3 migration, per-entry persisted_shipments validation via _SHIPMENT_FIELD_TYPES, and cleanup-path store sync — tests (a), (d), (f) GREEN**

## Performance

- **Duration:** ~18 min
- **Started:** 2026-05-23T13:35:00Z
- **Completed:** 2026-05-23T13:53:00Z
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments

- STORAGE_VERSION bumped from 2 to 3; v2->v3 migration branch added to _async_migrate_func — preserves submitted_tracking_numbers + quota_exhausted_until, seeds persisted_shipments as empty dict, emits WARNING with entry_id
- _SHIPMENT_FIELD_TYPES module-level constant + _async_load_store extension: per-entry type validation with one WARNING per invalid entry, populates self._restored_shipments with valid ShipmentData instances only (T-13.1-04 mitigation)
- _async_save_store lambda extended with persisted_shipments key using asdict(shipment) comprehension; async_cleanup_delivered appends _pending_shipments = new_data + await _async_save_store() after entity registry removal loop

## Task Commits

Each task was committed atomically:

1. **Task 1: Bump STORAGE_VERSION, add v2->v3 migration branch, fix asdict import** - `050764e` (feat)
2. **Task 2: Add _pending_shipments/_restored_shipments fields; extend _async_load_store** - `ec4c697` (feat)
3. **Task 3: Extend _async_save_store lambda + async_cleanup_delivered** - `84355d9` (feat)

## Files Created/Modified

- `custom_components/shop2parcel/coordinator.py` - STORAGE_VERSION=3, asdict import, v2->v3 migration branch, _SHIPMENT_FIELD_TYPES constant, _pending_shipments + _restored_shipments fields, extended _async_load_store, extended save lambda, cleanup store sync
- `tests/test_store_migration.py` - Stale passthrough test updated from v2 to v3 (Rule 1 auto-fix)

## Coordinator.py Line Ranges Changed

| Change | Location | Description |
|--------|----------|-------------|
| Import | line 22 | `from dataclasses import asdict, dataclass, field` |
| Constant | line 49 | `STORAGE_VERSION = 3` |
| New constant | lines 51-62 | `_SHIPMENT_FIELD_TYPES` dict (5 field→type entries) |
| Migration | lines 215-228 | `if old_major_version == 2:` branch in `_async_migrate_func` |
| __init__ | lines 271-272 | `self._pending_shipments` and `self._restored_shipments` fields |
| _async_load_store | lines 343-364 | Per-entry validation + _restored_shipments population |
| _async_save_store | lines 378-385 | `persisted_shipments` key added to async_delay_save lambda |
| async_cleanup_delivered | lines 472-473 | `self._pending_shipments = new_data` + `await self._async_save_store()` |

## Test Results After Plan 02

| Test | Status |
|------|--------|
| (a) test_migrate_func_v2_seeds_persisted_shipments | GREEN |
| (d) test_cleanup_removes_shipment_from_store[GmailCoordinator] | GREEN |
| (d) test_cleanup_removes_shipment_from_store[ImapCoordinator] | GREEN |
| (f) test_load_store_skips_corrupt_persisted_shipments_entry | GREEN |
| (b) test_shipments_saved_to_store_after_poll[both] | RED (Plan 03) |
| (c) test_restored_shipments_present_in_first_poll[both] | RED (Plan 03) |
| (e) test_fifo_cap_evicts_oldest_entry[both] | RED (Plan 03) |

Full test suite: **340 passed, 6 failed** (the 6 remaining RED tests are all expected — they require Plan 03 subclass changes in gmail_coordinator.py and imap_coordinator.py).

## Decisions Made

- `asdict` imported alphabetically in `from dataclasses import asdict, dataclass, field` — no runtime effect until Task 3 uses it, but importing early avoids a blocking error at the save step
- Module-level `_SHIPMENT_FIELD_TYPES` preferred over inline dict inside `_async_load_store` for reuse and to make the validation contract visible to static analysis
- The stale passthrough test `test_migrate_func_future_version_returns_data_unchanged` was fixed as a Rule 1 auto-fix (it still tested `old_major_version == 2` against `store.version = 2`, which now hits the v2->v3 migration branch)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated stale same-major passthrough test after version bump**
- **Found during:** Task 1 (running migration test suite after STORAGE_VERSION change)
- **Issue:** `test_migrate_func_future_version_returns_data_unchanged` set `store.version = 2` and called `_async_migrate_func(2, 0, ...)`. After STORAGE_VERSION bumped to 3, `old_major_version == 2` now hits the new v2->v3 migration branch instead of the passthrough path — the test fails for the wrong reason and loses its original intent.
- **Fix:** Updated test to `store.version = 3` and `_async_migrate_func(3, 0, ...)` so it correctly tests the same-major-future-minor passthrough at the current version.
- **Files modified:** tests/test_store_migration.py
- **Verification:** `pytest tests/test_store_migration.py -v -k "migrate"` — 6 passed
- **Committed in:** `050764e` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 — stale test)
**Impact on plan:** Necessary correctness fix. The test was written for v2 and became incorrect after the version bump. No scope creep — one line changed in the test file.

## Issues Encountered

None — all tasks executed cleanly. The stale passthrough test was caught by running the full migration test suite after Task 1, which is the prescribed verification step.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Base coordinator changes for Phase 13.1 are complete
- Plan 03 can now implement the subclass-specific changes in `gmail_coordinator.py` and `imap_coordinator.py`:
  - Seed `current_data` from `_restored_shipments` when `self.data is None` (R2)
  - Assign `_pending_shipments = trimmed` + `await _async_save_store()` at end of poll (R1/R6)
  - Apply FIFO cap (≤ MAX_SUBMITTED_TRACKING_NUMBERS) before assigning _pending_shipments (R6)
- Tests (b), (c), (e) will turn GREEN in Plan 03

## Self-Check

- STORAGE_VERSION = 3: line 49 confirmed
- v2->v3 migration branch: line 215 confirmed
- _SHIPMENT_FIELD_TYPES: lines 54-62 confirmed (definition + 3 usages)
- _pending_shipments field: line 271 confirmed
- _restored_shipments field: line 272 confirmed
- persisted_shipments in save lambda: line 381 confirmed
- _pending_shipments = new_data in cleanup: line 473 confirmed
- Commits: 050764e, ec4c697, 84355d9 all verified in git log

## Self-Check: PASSED

---
*Phase: 13.1-sensor-restore-on-restart-extend-dedup-store-to-persist-ship*
*Completed: 2026-05-23*
