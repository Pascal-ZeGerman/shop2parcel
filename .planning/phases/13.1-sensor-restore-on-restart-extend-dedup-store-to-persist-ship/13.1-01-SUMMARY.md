---
phase: 13.1-sensor-restore-on-restart-extend-dedup-store-to-persist-ship
plan: "01"
subsystem: tests
tags:
  - testing
  - ha-storage
  - tdd-red
  - migration
dependency_graph:
  requires: []
  provides:
    - "6 new RED test cases in tests/test_store_migration.py covering R1-R6"
    - "Updated stale future-major test targeting STORAGE_VERSION=3"
  affects:
    - "Plans 02 and 03 must turn these tests GREEN"
tech_stack:
  added: []
  patterns:
    - "Store.__new__ stub fixture for isolation (tests a, f)"
    - "@pytest.mark.parametrize over [GmailCoordinator, ImapCoordinator] for tests b-e"
    - "conftest helpers (setup_coordinator_with_data / setup_imap_coordinator_with_data)"
key_files:
  created: []
  modified:
    - tests/test_store_migration.py
decisions:
  - "Used Store.__new__ stub (no hass) for tests (a) and (f) — matches existing fixture"
  - "Replicated conftest helper calls inline for parametrized tests — avoids conftest modification"
  - "Import from tests.conftest mirrors existing pattern in test_diagnostics.py"
  - "test_cleanup_removes_shipment_from_store patches ParcelAppClient at coordinator.py path (not subclass)"
metrics:
  duration: "394 seconds (approx 7 minutes)"
  completed: "2026-05-23"
  tasks_completed: 3
  tasks_total: 3
  files_modified: 1
---

# Phase 13.1 Plan 01: TDD RED — Store Persistence and Restore Contracts Summary

**One-liner:** 6 RED test contracts for v2-to-v3 store migration, persisted-shipments save/restore, FIFO cap, and cleanup sync — locking what Plans 02 and 03 must implement.

## New Test Functions (6 added)

| Test Name | Requirement | Type | Status |
|-----------|-------------|------|--------|
| `test_migrate_func_v2_seeds_persisted_shipments` | R3/test-a | stub fixture (no hass) | RED (fails) |
| `test_load_store_skips_corrupt_persisted_shipments_entry` | R5/test-f | stub fixture (no hass) | RED (fails) |
| `test_shipments_saved_to_store_after_poll` | R1/test-b | parametrized x2 | RED (fails) |
| `test_restored_shipments_present_in_first_poll` | R2/test-c | parametrized x2 | RED (fails) |
| `test_cleanup_removes_shipment_from_store` | R4/test-d | parametrized x2 | RED (fails) |
| `test_fifo_cap_evicts_oldest_entry` | R1/test-e | parametrized x2 | RED (fails) |

Total new cases: **10** (2 non-parametrized + 4 × 2 parametrized)

## Updated Test (1 refactored)

| Test Name | Change | Status |
|-----------|--------|--------|
| `test_migrate_unknown_future_major_returns_empty` | `store.version = 2 → 3`; `_async_migrate_func(3, 0, ...)` → `(4, 0, ...)` | GREEN (still passes) |

## Test Count Verification

```
grep -c "^async def test_" tests/test_store_migration.py  →  11
```
- Original 5 tests: all GREEN
- Updated future-major test: GREEN
- 6 new tests (10 parametrized cases): all RED (failing as required)

## pytest Output Summary

```
PASSED  test_migrate_func_v1_drops_old_keys_and_seeds_submitted_tracking_numbers
PASSED  test_migrate_func_v1_preserves_missing_quota_exhausted_until_as_none
PASSED  test_migrate_func_v1_emits_warning_with_entry_id
PASSED  test_migrate_func_future_version_returns_data_unchanged
PASSED  test_migrate_unknown_future_major_returns_empty
FAILED  test_migrate_func_v2_seeds_persisted_shipments       (AttributeError: no 'version' attr — v2→v3 branch missing)
FAILED  test_load_store_skips_corrupt_persisted_shipments_entry  (AssertionError: _restored_shipments not populated)
FAILED  test_shipments_saved_to_store_after_poll[GmailCoordinator]   (async_delay_save not called at end of poll)
FAILED  test_shipments_saved_to_store_after_poll[ImapCoordinator]    (async_delay_save not called at end of poll)
FAILED  test_restored_shipments_present_in_first_poll[GmailCoordinator]  (seed from _restored_shipments missing)
FAILED  test_restored_shipments_present_in_first_poll[ImapCoordinator]   (seed from _restored_shipments missing)
FAILED  test_cleanup_removes_shipment_from_store[GmailCoordinator]   (_pending_shipments not assigned in cleanup)
FAILED  test_cleanup_removes_shipment_from_store[ImapCoordinator]    (_pending_shipments not assigned in cleanup)
FAILED  test_fifo_cap_evicts_oldest_entry[GmailCoordinator]   (FIFO trim not implemented)
FAILED  test_fifo_cap_evicts_oldest_entry[ImapCoordinator]    (FIFO trim not implemented)
```

**Result: 5 PASSED, 10 FAILED — exactly the expected RED/GREEN split.**

## No Production Code Modified

```
git diff custom_components/  →  (empty — 0 bytes)
```

This is confirmed: only `tests/test_store_migration.py` was changed in this plan.

## Contracts Locked for Plans 02 and 03

The 6 new test functions establish the exact contracts that Plans 02 and 03 must implement:

| Contract | Test | Plan |
|----------|------|------|
| `_async_migrate_func(2, 0, ...)` returns `persisted_shipments: {}` | test-a | 02 |
| `_async_load_store()` populates `_restored_shipments`, skips invalid with WARNING | test-f | 02 |
| `_pending_shipments` + `_async_save_store()` at end of `_async_update_data` (both coordinators) | test-b, test-e | 03 |
| Seed `current_data` from `_restored_shipments` when `self.data is None` | test-c | 03 |
| `async_cleanup_delivered` assigns `_pending_shipments = new_data` + saves store | test-d | 02 |
| FIFO cap at `MAX_SUBMITTED_TRACKING_NUMBERS = 1000` on `_pending_shipments` | test-e | 03 |

## Deviations from Plan

None — plan executed exactly as written.

The `_setup_coordinator` helper function was added inline in the test file (not in conftest.py) to provide a clean adapter over the two existing conftest helpers — this is consistent with the plan's instruction "Do not modify conftest.py in this task."

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes were introduced — this plan only adds test code.

## Self-Check: PASSED

- [x] `tests/test_store_migration.py` exists and modified
- [x] Commits exist: fae0428 (Task 1), e068b19 (Task 2), 325d29a (Task 3)
- [x] 5 pre-existing tests: GREEN
- [x] 10 new test cases: RED
- [x] `git diff custom_components/` is empty
- [x] `grep -c "^async def test_"` returns 11
- [x] Collection exits 0 with 15 items
- [x] 4 parametrize decorators present (one per parametrized test)
